rootProject.name = 'ExampleApp'

include ':react-native-unified-contacts'

// Use when inside of react-native-unified-contacts and doing development.
// project(':react-native-unified-contacts').projectDir = new File(rootProject.projectDir, '../../android')

// Use when you have cloned ExampleApp without the parent react-native-unified-contacts.
project(':react-native-unified-contacts').projectDir = new File(rootProject.projectDir, '../node_modules/react-native-unified-contacts/android')

include ':app'
